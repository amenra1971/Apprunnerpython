version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      -  pipenv --python /path/to/python3.11.8 install
run:
  command: pipenv run python guess_the_number.py

